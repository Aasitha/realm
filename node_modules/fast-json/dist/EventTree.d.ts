import { FastJsonData, FastJsonListener, FastJsonPath } from './types';
export declare class EventTree {
    private tree;
    private customPathSeparator;
    private expandedTree;
    private node;
    constructor(rootName: string, customPathSeparator?: string);
    down(name: string): void;
    up(): void;
    hasNode(name: string): boolean;
    hasListener(): boolean;
    emit(data: FastJsonData): void;
    on(path: FastJsonPath, listener: FastJsonListener): void;
    reset(): void;
    private parsePath;
}
